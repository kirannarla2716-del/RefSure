{
  "name": "refsure-security-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --project refsure-rules-test --only firestore,storage \"mocha --timeout 10000 rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0",
    "mocha": "^11.0.0"
  }
}
